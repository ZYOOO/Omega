# Omega Shared Package

Shared TypeScript types, API schemas, and client utilities can move here when the web app, desktop shell, and injected preview agent need the same contracts.

Current v0Beta keeps the active API clients in `apps/web/src` until the desktop and preview packages need them.
